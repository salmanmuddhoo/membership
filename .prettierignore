# dependencies / lockfiles (Dependabot regenerates these; never format)
pnpm-lock.yaml
package-lock.json
yarn.lock
node_modules/

# build / generated
dist/
.astro/
.vercel/
# Written by scripts/generate-openapi.ts. Prettier collapses its short arrays,
# which makes `pnpm openapi:check` see drift the moment `format:fix` runs — two
# tools disagreeing about one generated file. The generator owns its format.
docs/openapi.json

# env / OS
.env
.env.*
.DS_Store

# no TOML parser in prettier
*.toml
