# --- Application (browser + server) ---------------------------------------
# Copy this file to `.env` and fill in the values from your Supabase project
# (Project Settings -> API). The `PUBLIC_` prefix exposes these to the browser,
# which is expected for the Supabase URL and anon (publishable) key.
PUBLIC_SUPABASE_URL="https://your-project-ref.supabase.co"
PUBLIC_SUPABASE_ANON_KEY="your-anon-public-key"

# --- Local scripts / CI only (NEVER expose to the browser) ----------------
# Used only by `pnpm create:user` (scripts/create-user.mjs) to provision staff
# accounts. This is a SECRET: do not add a PUBLIC_ prefix and never commit it.
# SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"
